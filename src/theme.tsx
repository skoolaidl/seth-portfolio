import { createTheme } from '@mui/material';

const theme = createTheme({
    palette: {
        primary: {
            main: '#FDC5F4'
        },
        secondary: {
            main: "#CBCBCB"
        }
    }
});

export default theme;